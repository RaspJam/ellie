faster_whisper
# NOTE: tflite-runtime does not seem to work so for now it is commented out
#tflite-runtime # sometimes pip3 install --extra-index-url https://google-coral.github.io/py-repo/ tflite_runtime
openwakeword
colorama
pyaudio
numpy==1.24.0
